import styled from '@emotion/styled/macro'
import React from 'react'
import { spaces } from '../theme'

export default function Breadcrums() {
  return (
    <Container>Breadcrums</Container>
  )
}

const Container = styled.div`
  margin-bottom: 30px;
`